name: CI

# This repo's default branch is master - there is no main branch on origin.
# The filter used to list only [main], so no push or pull request ever matched and
# the workflow never ran; that is how 36 files drifted out of Prettier format
# unnoticed. master is listed first so it triggers today, and main is kept so a
# future rename does not silently disable CI again.
on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:

jobs:
  frontend-checks:
    name: Frontend Quality & Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            frontend/package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Format check (Prettier)
        run: npx prettier --check .

      - name: Lint (ESLint)
        run: npx eslint .
        working-directory: frontend

      - name: Run frontend unit & store tests (Vitest)
        run: npx vitest run

  backend-checks:
    name: Rust Quality, Docs & Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Check formatting (cargo fmt)
        run: cargo fmt --check

      - name: Clippy linter
        run: cargo clippy --all-targets -- -D warnings

      - name: Documentation check (cargo doc)
        run: cargo doc --no-deps

      - name: Run Rust tests
        run: cargo test
